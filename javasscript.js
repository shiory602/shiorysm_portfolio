// let products = ['apple', 'milk', 'bread', 'bag']

// for (let i = 0; i < products.length; i++) {
//   let word;
//   let p = document.getElementById('title')
//   // p.insertAdjacentHTML("beforeend", ' Hide');
//   if(i = products.length-1) {
//     word += ' ' + products[i] + '.';
//     p.insertAdjacentHTML("beforeend", word);
//   }
//   word += ' ' + products[i] + ',';
//   console.log(products[i]);
//   p.insertAdjacentHTML("beforeend", word);
// }


// let products = ['apple', 'milk', 'bread', 'bag'];
// console.log(products.join(', ') + '.');
// let word = products.join(', ') + '.'
// let p = document.getElementById('title')
// p.insertAdjacentHTML("beforeend", word);